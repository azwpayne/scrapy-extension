# R93 SPEC

A Kafka queue-length probe failure remains the public outcome even if closing its private temporary consumer raises a control exception; successful probes retain close-control visibility.
