# DEEP-INSIGHT 2026-08-23 — 八维度对抗复核综合报告

> 生成日期：2026-08-23。输入：8 个维度审查员 + 各自对抗复核员的完整结果 JSON（D1 并发与生命周期 / D2 消息投递语义 / D3 安全与脱敏 / D4 设置与校验契约 / D5 测试健康与覆盖 / D6 文档一致性 / D7 打包与 CI / D8 公共 API 与类型）。本报告为综合成报阶段的唯一产出。

---

## 1. 执行摘要（数字面板）

| 指标 | 数值 | 说明 |
|---|---|---|
| 审查维度 | 8 / 8（无缺失覆盖维度） | 全部维度 ok 且附 skeptic 裁决 |
| 候选发现（原始） | **29** | D1:4、D2:3、D3:3、D4:6、D5:4、D6:2、D7:2、D8:5 |
| 复核确认（原始） | **28** | skeptic 全部出勤，无缺失裁决 |
| 复核确认（合并后独立条目） | **23** | 跨维度同根因合并 3 组（吸收 5 条，保留全部来源 id） |
| 驳回（REFUTED） | **1** | D7-1（pyproject.toml.orig sdist 泄漏主张被实证推翻） |
| 降级（DOWNGRADED） | **0** | 无 adjusted_severity 低于原始定级 |
| 账本重复（DUPLICATE-LEDGER） | **0** | 全部核验 R1–R140 |
| 未复核（skeptic 缺失） | **0** | 无 |
| **P0** | **0** | — |
| **P1** | **1** | R141-F1（快照恢复围栏绕过，端到端复现） |
| **P2** | **8** | 含 2 条红测定性合并条目 |
| **P3** | **14** | 含 1 条 R138-F5 DIRTY-BLOCKED 再确认 |

**一句话结论**：唯一 P1 是 queue 快照恢复语义的可靠性缺陷（启动期存储解析瞬时失败 → close 用空 manifest 覆盖权威检查点）；4 个红测全部定性为**测试侧缺陷**（3 过期断言 + 1 harness 错位，零产品缺陷），修复它们即可恢复绿基线；其余为设置面 fail-fast 缺口（Redis/Kafka）、脱敏启发绕过、fan-out v2 迁移契约缺口与文档/公共 API 契约失真。

**跨维度合并明细**（同 file + 同根因类）：

| 合并条目 | 吸收来源 | 合并理由 |
|---|---|---|
| R141-F2 | D1-1 ⊇ D5-1、D5-2、D5-3 | 同一文件 tests/test_dupefilter_connector_closure.py、同一根因类（红测 1/2/3 过期断言），D1 与 D5 逐测试独立定性且结论一致、证据互补 |
| R141-F3 | D1-2 ⊇ D5-4 | 同一文件 tests/test_scheduler_pipeline_async_coverage.py、同一根因类（红测 4 harness 错位），D2 notes 佐证 |
| R141-F8 | D8-1 ⊇ D2-3 | 同一文件组 queue/strategies/_names.py + schedule/scheduler.py + migration-guide，同一根因类（fan-out legacy_v1 排空在 settings 面不可达 / v2 默认翻转缺契约） |

---

## 2. 基线状态

| 门禁 | 状态 |
|---|---|
| ruff | clean |
| mypy --strict src | clean |
| pytest | ≈7979 passed / **4 failed** / 55 skipped |

4 个失败（种子即本次审查对象）：

1. `test_dupefilter_connector_closure.py::test_legacy_handoff_is_thread_affine_and_stale_receipts_are_not_reused`
2. `test_dupefilter_connector_closure.py::test_clear_retire_marks_legacy_receipt_and_clears_volatile_state`
3. `test_dupefilter_connector_closure.py::test_filter_backend_failure_is_static_and_does_not_expose_driver_text`
4. `test_scheduler_pipeline_async_coverage.py::test_scheduler_close_queue_worker_retries_observer_and_releases_manager`

---

## 3. 确认风险总表（P1 → P3）

严重度均经对抗复核确认（adjusted_severity 与原始一致）。列：建议账本 id / 合并来源 / severity / file:lines / 根因类 / 风险 / 建议修复 / 账本状态。

### P1

| id | 来源 | Sev | file:lines | 根因类 | 风险 | 建议修复 | 账本状态 |
|---|---|---|---|---|---|---|---|
| **R141-F1** | D2-1 | **P1** | `src/scrapy_extension/queue/queue.py:2099-2103, 1937-1972, 2040-2095` | snapshot-restore-resolution-failure-bypasses-fence | 启动期快照存储**解析**瞬时失败被非 strict `_snapshot_storage` 吞掉并返回 None（与"后端不支持存储"不可区分）→ `_restore_snapshot` 直接 return、不设围栏；storage 恢复后 close 走 `_persist_snapshot` 用当前（空）状态覆盖权威 manifest 并删除从未读过的 legacy checkpoint。**端到端复现**：149 字节 manifest → 重启窗口一次解析失败 → 恢复后 close → 空 manifest（state=None），有状态策略（delay/time_wheel/round_robin/ring_buffer）全部持有项**静默永久丢失**，无 fence、无 reset_snapshot 提示 | 在 `_snapshot_storage` 保留 unsupported 与 resolution_failed 的区分：resolution 失败时对 `_restore_snapshot` 内设 `_snapshot_persistence_fenced = True`（与同函数读失败分支 2127-2132 同构）；`NotImplementedError`（storage-incapable）仍为无害 no-op，操作员可用既有 reset_snapshot() 显式授权替换 | NEW（R133-F3 LANDED 只覆盖 tombstone 读失败与 legacy 读失败两臂） |

### P2

| id | 来源 | Sev | file:lines | 根因类 | 风险 | 建议修复 | 账本状态 |
|---|---|---|---|---|---|---|---|
| **R141-F2** | D1-1 ⊇ D5-1/D5-2/D5-3 | P2 | `tests/test_dupefilter_connector_closure.py:150-162, 406-418, 426-437` | stale-test-assertion-dupefilter-receipt-protocol | 红测 1/2/3 的三处断言与现行（自洽的）收据协议矛盾（详见 §4 定性表）。若误判为产品缺陷并"修复"会产生真实 P1 级破坏：settle 时删 marker = 每次成功入队的 URL 永久失去去重（无限重爬）；降级路径强行记账 = 未写 marker 却引入 forget 补偿。同时阻塞绿基线（7979/4） | 改测试不改产品：(1) `remove_calls == 0` 并补断言 settle 后同指纹再次 request_seen 为 True；(2) setup 换 URL（或去掉第一次 request_seen）获得真实 miss+reservation；(3) `_legacy_reservations == {}`，把"不泄露 driver 文本"检查移到 monitor 事件（on_error 静态错误）与日志 | NEW（未跟踪新测试文件，账本无对应条目） |
| **R141-F3** | D1-2 ⊇ D5-4 | P2 | `tests/test_scheduler_pipeline_async_coverage.py:54-70, 396-428` | test-harness-deferred-delivery-misalignment | 红测 4 harness 错位（详见 §4）。产品 close 观察者重试契约（scheduler.py:2672-2683）真实存在且正确，但被该测试**零覆盖**（首个 addCallbacks 实际拿到普通 Deferred 而成功，拒绝/重试分支从未进入）——未来回归无测试可捕获 | 让 submit 在有预置对象时 pop 返回（或 monkeypatch `_submit_thread`），随后 `_complete_worker(workers, 0)` 真实驱动 queue.close 并验证重试后观察者链 | NEW |
| **R141-F4** | D3-1 | P2 | `src/scrapy_extension/exceptions/base.py:98-110`；`src/scrapy_extension/backends/_redaction.py:94-105` | redaction-schemeless-userinfo-bypass | 无 scheme 的 `user:pw@host:port` 使 urlsplit 的 username 检查成为死代码（无 `scheme://` 时 netloc/username 恒 None），两层脱敏启发均放行；**实测** `BackendConnectionError('Failed to connect to user:hunter2@queue.internal:5672 …', backend_type='user:hunter2@…')` 在 `str(e)` 与 `.backend_type` 双双原样保留，且 kv/URI 回退正则（只认 password/token 等关键词与 `scheme://`）也不匹配。bundled 公共路径仍受静态消息终界保护，暴露需插件后端/未装饰构造路径 → P2 而非 P1 | 在 `_looks_sensitive_text`（exceptions/base.py）与 `_diagnostic_repr`（backends/_redaction.py）两处增加结构性前置检查：`re.match(r"^[^/\s:@]+:[^/\s@]*@", value)`（任何 `/` 之前的冒号+`@` 即视为传输凭据材料），或要求 `"//" in value` 才信任 urlsplit、`x:y@` 前缀一律按传输材料处理 | NEW（R134 CLEAN 裁决先于本 WIP 对两个 helper 的完整重写） |
| **R141-F5** | D4-1 | P2 | `src/scrapy_extension/settings/redis.py:394-401, 469-479` | redis-blank-credential-accepted | Redis `password/username/sentinel_password/sentinel_username` 接受空白值（全部 9 个兄弟后端均 fail-fast 拒空白），**实测** `RedisSettings(host="localhost", username="   ", password="")` 构造成功并原样进入 redis-py：空 password 时 `if password or username` 判空静默跳过 AUTH（操作者以为配置了认证实际未认证）；空白 username 触发连接期 opaque ACL 失败；且 `has_authentication` 把空白算作认证意图强制远程 TLS（配置一半生效一半不生效） | strip-aware 校验（同 MongoDB R32-A 形）：username/sentinel_username `not value.strip()` → ConfigurationError；password/sentinel_password 用 `get_secret_value().strip()` 判空（错误不带 setting_value）；同步进 `validate_redis_transport_security` | NEW（R30 仅 sentinel_master_name；R31/R32 仅 MongoDB） |
| **R141-F6** | D4-2 | P2 | `src/scrapy_extension/settings/kafka.py:370-384, 551-578`；`src/scrapy_extension/backends/kafka.py:1179-1191, 1213-1216` | confluent-mode-drops-validated-tls-material | **实测** CONFLUENT 模式 + `ssl_cafile/cert/key`（含 security_protocol=PLAINTEXT 或 SSL）通过 settings 校验，但后端 CONFLUENT 早退 dict（固定 SASL_SSL/PLAIN）**不含** ssl_* 物料：consumer/admin 永远拿不到 CA/证书，producer 仅在另设非 PLAINTEXT 协议时经 `_build_common_config` 残留 ssl_*，行为不一致。私有 CA 的 Confluent 端点：钉根证书"看似成功"实际未钉住（安全配置静默失效）或 connect 期 TLS verify 不透明失败；security_protocol 被静默覆盖。违背本项目 ES CLOUD 的 fail-fast 先例 | model_validator：CONFLUENT 模式下配置 ssl_cafile/cert/key 或显式 security_protocol≠SASL_SSL → ConfigurationError（"unsupported in confluent mode because the backend builds a fixed SASL_SSL client"）；或在该分支真正透传 ssl 物料 | NEW（R26-E/R28-C 仅覆盖 localhost/空端点维度） |
| **R141-F7** | D6-1 | P2 | `.github/CHANGELOG.md:326-340`（对照 :45-51；`src/scrapy_extension/backends/dynamodb.py:718-745, 12-13`） | changelog-stale-batched-clear-description | Unreleased→Breaking 残留已被取代的 DynamoDB "BatchWriteItem 限界 clear"（每批 25 条、7 次 full-jitter、8 次提交、malformed BatchWrite 错误面）描述；当前实现为逐行条件 `DeleteItem(ALL_OLD)`+revision 校验（全 src 0 处 BatchWrite/batch_writer）。同段 :45-51 新 bullet、README、runbook、migration-guide 均描述 DeleteItem 路径——同一 Unreleased 段内两种互斥实现。操作员会按错误预算与错误面维护 clear | 发布前删除或改写该 bullet 为最终行为（数量=Scan 观察行数、仅受 botocore 自身重试约束、失败面为 condition loss/malformed delete/重复游标/SDK 失败），与 :45-51、README/runbook/migration-guide 对齐 | NEW（R85/R119 为不同根因） |
| **R141-F8** | D8-1 ⊇ D2-3 | P2 | `src/scrapy_extension/queue/strategies/_names.py:26-27, 74-95, 128`；`schedule/scheduler.py:973-987`；`schedule/_queue_config.py:80-93`；`priority.py:141-149`；`work_stealing.py:162-171`；`docs/migration-guide.md:577-583`；CHANGELOG [Unreleased] | fanout-v2-default-flip-unreachable-drain | WIP 将 fan-out 物理名默认从 legacy 冒号名翻转为 v2（前缀 `scrapyext-v2-{ns}-`），调度器唯一构桥点不传 `name_generation`，全部 SCRAPY_QUEUE_* 键无对应旋钮（grep 全 src 无 SCRAPY_QUEUE_NAME_GENERATION）——(a) legacy-colon 后端（redis/mongodb/es/pulsar/rabbitmq）上 settings 驱动的 priority/work_stealing 部署升级后**静默换队列**：读到深度 0、旧 `{queue}:p{n}` backlog 永不再被读（v2 不双读）且无启动告警；(b) migration-guide/runbook 指示的 `name_generation="legacy_v1"` 排空在受支持 settings 面**不可达**（复核更正：`BackendScheduler.__init__(queue_strategy=…)` 公共注入缝可程序化达成，但文档主指令按字面不可执行）；(c) CHANGELOG 无 Breaking 条目（违反 STABILITY.md "Experimental 变更也需 changelog notice" 自定契约）；(d) priority/work_stealing docstring 仍自称 "backlog-compatible"（v2 默认下为假） | 补 CHANGELOG [Unreleased] Breaking 条目（默认 v2、不 dual-read、排空步骤与现状说明）；增加受校验旋钮 `SCRAPY_QUEUE_NAME_GENERATION ∈ {v2, legacy_v1}`（SQS 的 `SCRAPY_SQS_QUEUE_NAME_GENERATION` 已有先例模式）；或 v2 默认首次绑 legacy-colon 后端时一次性迁移 warning；修正两处 "backlog-compatible" docstring | NEW（R138-F1 LANDED 仅覆盖冒号判别器哈希这一更窄变更） |
| **R141-F9** | D8-2 | P2 | `src/scrapy_extension/queue/queue.py:155-163, 251-253`；`src/scrapy_extension/pipeline/pipeline.py:158-171, 1457` | documented-attribute-missing-on-instance | Stable 层组件类 docstring 声明的 Attributes 在实例上不存在：`BackendQueue.serializer`/`BackendQueue.spider`（`__init__` 仅赋 `connection_manager/queue_name/_spider`，全文件 0 处 `self.serializer`）、`BackendPipeline.serializer`（仅 `_serializer`）。**实测**三个访问均 AttributeError；下游按文档（Stable 公共属性）使用直接崩溃 | 风险最小：从两个类 docstring 的 Attributes 删除不存在条目；或补只读 property（`spider`、`serializer`）与文档对齐 | NEW（docstring 与 HEAD 相同、非 WIP 新引入，但当前工作树审查属真实契约缺陷；R35-R140 无条目） |

### P3

| id | 来源 | Sev | file:lines | 根因类 | 风险 | 建议修复 | 账本状态 |
|---|---|---|---|---|---|---|---|
| **R141-F10** | D1-3 | P3 | `src/scrapy_extension/spider/spider_mixin.py:1651-1655`；`queue/queue.py:382-387`（对照 `:965-991, 258, 1319-1349`） | set-monitor-upgrade-drops-pop-rate-window | R138-F5（DIRTY-BLOCKED）再验证仍未修复并**实测复现**：early-setup（无 crawler）构造的队列 monitor 升级为 ScrapyStatsMonitor 后 `_pop_rate_window_s` 仍冻结 60.0（期望 `SCRAPY_MONITOR_POP_RATE_WINDOW_S=300.0`；对照组全新构造得 300.0）。`monitor, _ = self._resolve_queue_monitor()` 丢弃 window；`set_monitor` 只更新 `_monitor` 不更新窗口（构造时固定）→ 该配置在升级路径是死配置，queue/pop_rate gauge 按错误窗口计算。仅观测性 | `BackendQueue.set_monitor` 增加可选 `pop_rate_window_s: float \| None = None`（None 保持不变）并在 spider_mixin.py:1653 改 `monitor, window = self._resolve_queue_monitor()` 透传；或新增专用 `set_pop_rate_window()`；保持既有调用方兼容 | R138-F5 DIRTY-BLOCKED 再确认（2026-08-23 工作树实测复现；建议与 R138-F4/F6 等 spider_mixin/connectors DIRTY 项同批清偿） |
| **R141-F11** | D1-4 | P3 | `src/scrapy_extension/dupefilter/dupefilter.py:1840-1847, 1943`（except 分支实际 ~1853-1862） | legacy-add-before-receipt-publish-baseexception-window | legacy `request_seen` 在 `filter.add`（marker 已写入后端，:1943）→ 收据发布（:1840-1847）之间存在未补偿 BaseException 窗口：**实测**（monkeypatch 注入 KeyboardInterrupt）marker 在过滤器中、`_legacy_reservations=={}`、无 retry allowance，`forget()` 无收据时 no-op，调度器侧 `dedup_reserved` 尚为 False（True 预 arm 在 request_seen 返回后，scheduler.py:3008/3065）→ 外层清理不调 forget → **幽灵 marker 永久压制该 URL**（本进程内直到 clear）。事务性路径对同类窗口已有 owner-intent 协议（:1795-1808 + `_compensate_interrupted_decision`），legacy 路径无对称保护——项目自身标准的边界缺口。窗口极窄（数条字节码），单次命中丢一个 URL | legacy 路径引入对称"发布前 intent"：filter.add 前在 lifecycle 锁内登记 pending intent（request 身份+fingerprint），BaseException 时若 add 可能已生效则按 forget 语义授予一次性 retry allowance（复用 `_grant_retry_allowance` 的 reserved-slot 机制）；至少在 except 分支记录未决指纹供文档化的 close/clear 兜底 | NEW（R129/R130 系列未覆盖 add→发布间隙） |
| **R141-F12** | D2-2 | P3 | `src/scrapy_extension/backends/kafka.py:2435-2438, 2470-2505`（remove 实际 :2439，watermark 写 ~2517） | kafka-ack-postcommit-interruption-attempt-leak | `consumer.commit` 成功（:2475-2481）后、提交后簿记（watermark+`_finish_attempt_locked`，:2505+）前落入进程控制异常 → attempt 表项残留；重试同一 token 时 in-flight 守卫（:2435-2437，偏移已于提交前移除）提前 return 不终经。**实测复现**：`_active_attempts/{by_partition}` 残留 `{('topic',0,5):1}`，legacy 裸 ack（token=None）在该 topic-partition 被持续 typed 拒绝（QueueError）直到 rebalance/reconnect。影响面：外部 legacy 调用方 + 小内存泄漏；token ack 不受影响、相邻 ack 重复提交同一 offset broker 幂等无害 | 在 `token.offset not in in_flight` 提前 return 前调用 `self._finish_attempt_locked(token, handles)`（偏移已不在 in-flight 且无恢复记录则不可能再成功结算）；保留 handles 身份校验以免波及换代后映射 | NEW（R57/R91/R139-F1 均为不同根因） |
| **R141-F13** | D3-2 | P3 | `src/scrapy_extension/exceptions/base.py:694-695`（对照守卫 :284） | empty-secret-needle-interleaves-redaction | `ConfigurationError(setting_name='password', setting_value='' 或 SecretStr(''))` → `safe_message.replace("", '***REDACTED***')` 在**每个字符间**插入掩码（**实测** `***REDACTED***I***REDACTED***n***REDACTED***v…`）；主敏感值循环 :284 有 `if not value: continue` 守卫而 secret 分支没有。过度假红/诊断破坏而非泄漏；bundled 校验器先拒空 → latent（插件/未来调用方） | 镜像守卫：`if secret is not None and secret != ""`；补 SecretStr('')/空串焦点测试 | NEW（WIP 重写 ConfigurationError 引入） |
| **R141-F14** | D3-3 | P3 | `src/scrapy_extension/backends/sqs.py:394-401` | sqs-ack-token-repr-renders-receipt-handle | `_SqsAckToken.__repr__` 注释声称 mask "credential-shaped opaque handles"，**实测**真实形态 receipt handle（`AQEBMASsILlagmFv0JkRJjY4RkI=`）原样渲染（无敏感关键词/scheme/userinfo 命中任何启发）；receipt handle 授权 delete/visibility 变更（非 AWS 凭据，sigv4 仍需），repr 类诊断（pdb/错误报告器）会发布活句柄；与 pulsar.py:220-227 对 message_id 无条件 mask 的先例不一致 | `receipt_handle=<redacted>` 无条件 mask（镜像 Pulsar），或附加结构化 opaque-token 检查（base64-ish、长度≥40、无空格） | NEW（R115 为不同机制） |
| **R141-F15** | D4-3 | P3 | `src/scrapy_extension/settings/_redacted.py:34-236`；对照 `settings/redis.py:60-66, kafka.py:147-153, elasticsearch.py:309` | settings-error-message-safelist-coverage-drift | 静态 f-string ConfigurationError 消息（Redis 地址错误、Kafka 机制×凭据 6 变体、ES basic-auth 2 变体）不在 `_SAFE_SETTINGS_CONFIGURATION_MESSAGES` → **实测三例**公开 settings 路径全部坍缩为泛化 "Settings contain an invalid configuration value."（setting_name 保留）；runbook 诊断表引用的 `'Redis setting … contains an invalid address'` 症状文本经公开路径永不出现（connect 期又被 `_REDIS_SAFE_CONFIGURATION_MESSAGES` 边界再替换）——文档-诊断-实现三方漂移。R67 根类复发（SPEC-round68 曾 DEFER 19 个 f-string 站点为挂账项） | 把静态变体补入 safe-list（Redis host/port/replicas/sentinels/cluster_startup_nodes + index 后缀全变体；Kafka 6 机制×字段；ES 2 missing 变体）+ 契约测试：枚举 settings/*.py 静态消息字符串对比清单 | NEW 实例（R67-LANDED 同根类；LEDGER 无对应行） |
| **R141-F16** | D4-4 | P3 | `src/scrapy_extension/settings/rabbitmq.py:260-267`；`backends/rabbitmq.py:839-860`；次要：`settings/mongodb.py:962` | rabbitmq-cluster-config-dead-and-half-applied | (1) `cluster_node_type` 纯死配置（grep 全 src 零消费；R25-H 曾以同理由删 RocketMQ 死配置并留 "do NOT re-add" 墓碑）；(2) **实测** `mode='standalone' + cluster_nodes=[…]` 通过校验：`validate_rabbitmq_connection` 把节点计入 endpoint_hosts（强制 TLS/禁 guest）但 `_connect_standalone` 只用 host:port——安全分类与实际连接面脱节，多节点配置静默退化为单点（对照 Redis 的 "Non-selected topology nodes fail rather than being ignored" 契约）；(3) 次要同类：mongodb `atlas_cluster_name` 接受后零消费（仅出现于错误文案） | 删除 `cluster_node_type`（留墓碑注释）；`validate_mode_requirements` 对 `mode==STANDALONE and cluster_nodes` raise ConfigurationError（与 Redis 模式意图拒绝同形） | NEW（R25-H 同根类新实例） |
| **R141-F17** | D4-5 | P3 | `src/scrapy_extension/settings/kafka.py:440-443`；`backends/kafka.py:1980, 2830` | kafka-group-id-blank-accepted | **实测** `KafkaSettings(group_id="   ")` 构造成功，无 validator；后端三处原样传入 KafkaConsumer，`_capture_connection_snapshot` 再校验清单不含 group_id。兄弟后端同语义身份字段全部拒空白（RocketMQ consumer_group、Pulsar subscription_name 1-255 ASCII、Redis namespace、MongoDB database）。空白组名到 broker 才失败（可能首次 poll 期而非 connect 期，更晚更不透明）或组身份意外分叉 | field_validator：非 str 或 `not value.strip()` → `ConfigurationError('Kafka group_id must be a non-empty string.', setting_name='group_id')` + RED 测试 | NEW（R27-R32 空白清扫未覆盖） |
| **R141-F18** | D4-6 | P3 | `src/scrapy_extension/settings/kafka.py:440-455, 327-333, 346-356`；`backends/kafka.py:417-425, 927-933` | kafka-settings-document-unavailable-values | (1) `enable_auto_commit=True` 字段描述呈现为可用选项且设置层**实测**接受，但每个真实 KafkaBackend 构造即拒——失败分层与项目 fail-fast 纪律不一致（acks/num_partitions 同类策略均在 settings 层拒）；(2) `security_protocol` Literal/描述列出 SASL_PLAINTEXT、`sasl_mechanism` 列出 OAUTHBEARER，但 validator 恒拒（有意收留以产生定向错误，设计合理）且无凭据时错误文案会误导未配 SASL 的用户；描述未告知这些值恒不可用 | settings 层 model_validator：`enable_auto_commit is not False` → ConfigurationError（复用后端文案并入 safe-list）；字段描述改 "must remain False; the queue ack contract requires manual commit"；两个恒拒值描述加标注 | NEW |
| **R141-F19** | D6-2 | P3 | `README.md:1096`；`src/scrapy_extension/spider/spider_mixin.py:138-142, 235-238, 1637-1639, 1878-1880, 1991-1993`（复核补 :3117-3118） | readme-forbids-init-setup-backend-vs-error-messages-demand-it | README（Per-Spider Settings）："Do not … call setup_backend() from `__init__`"；但类 docstring 的 Example 恰在 `__init__` 调 `setup_backend()`、**4 处**运行时错误消息指示 "Call setup_backend() in `Class.__init__()`"、from_crawler docstring 明言早设路径受支持。两条路径实际都能运行（from_crawler 幂等 re-finalize，聚焦测试 4 passed 佐证）——同公共面相反指引，文档可信度受损、易引出多余双重 from_crawler | 二选一对齐：README 改为 "automatically by from_crawler(); early `__init__` call is tolerated (idempotent) but unnecessary"；或更新示例与 4 处错误消息改为指示"直接构造后调用" | NEW（R136-F1 是早设路径断路器缺陷，非文档矛盾） |
| **R141-F20** | D7-2 | P3 | `.github/workflows/ci.yml:571-576` | ci-audit-report-never-uploaded | "Publish audit report" 步骤（if: always()，注释自称 "for artifact retention"）把 `audit-report.txt` 写到临时 runner 磁盘且 stdout/stderr 均重定向；grep 全 workflows 无 upload-artifact——audit 失败（if: always() 正为之设计）时诊断材料随 VM 销毁。损失有限（前一步骤 `uv audit --locked --ignore $waiver` 未重定向，日志仍可见） | 补 `uses: actions/upload-artifact@v4`（if: always(), path: audit-report.txt）；或删除该步骤及误导注释 | NEW |
| **R141-F21** | D8-3 | P3 | `src/scrapy_extension/dupefilter/dupefilter.py:1355-1361, 1384-1387, 1413-1425` | new-async-lifecycle-apis-return-any | WIP 新增公共 `open_async/clear_async/release_async` 全部标注 `-> Any`（内部帮助函数 `tuple[Any, Any]`），而 `defer_to_thread_ordered` 精确返回 `tuple[Deferred[_T], Deferred[_T]]`、同类 `get_queue_async` 已精确标注 `Deferred[BackendQueue]`——下游 strict mypy 对 `df.open_async()` reveal_type 为 Any，回调/errback/超时语义全失检查，掩盖 addCallback/addErrback 签名错误（mypy --strict 全绿但弱） | TYPE_CHECKING 导入 Deferred，三个方法标注 `Deferred[None]`（帮助函数 `tuple[Deferred[None], Deferred[None]]`）；模块已启用 `from __future__ import annotations`，无需运行时导入 | NEW（WIP 新增方法） |
| **R141-F22** | D8-4 | P3 | `src/scrapy_extension/backends/connectors/__init__.py:21-23`；`connectors/_manager.py:953, 2659, 2975-2992` | exported-lease-breaker-apis-tierless-changelogless-any-typed | `ConnectionManagerLease`、`release_manager_acquire`（exact 双语义 + `owner: Any` 鸭子分派）、`apply_scrapy_breaker_policy`（`settings: Any`）、`acquire_lease` 已导出/公共化，但 STABILITY.md（"A name's tier comes from this document"）无层级、CHANGELOG 无条目、README/runbook 无用户面提及——按仓库自定契约处于"公共但无层级"悬空态，后续改名/删减即未声明破坏。复核更正：**非 WIP 新增**（HEAD 已存在，connectors/__init__.py 与 HEAD 逐字节相同），属预存在缺口 | STABILITY 组件/fresh 表指定层级（建议 Experimental）+ CHANGELOG Added 条目；`settings: Any` 收紧为最小 Protocol（含 get）、`owner: Any` 收紧为 `ConnectionManager \| ConnectionManagerLease` 或 close/release Protocol | NEW（R135-F2/R136 记录功能落地，未覆盖契约缺口） |
| **R141-F23** | D8-5 | P3 | `src/scrapy_extension/monitor/base.py:119-121`（对照 :307, :324） | monitor-docstring-hook-list-incomplete | Monitor 类 docstring 的 "Hooks (all no-ops by default):" 清单（15 个）遗漏实际定义的 `on_buffer_depth`（BatchedStorageStrategy 未落盘 gauge）与 `on_delay_depth`（DelayQueueStrategy 持有项 gauge）；STABILITY 引用 hook set 为 additive，该清单是事实上的契约清单——自定义 Monitor 用户会漏覆盖两个 gauge，告警面不完整。纯文档面 | 在 Hooks 清单补两行（与既有条目风格一致） | NEW |

---

## 4. 四个红测定性表（最终判定）

**最终判定：4 / 4 均为测试侧缺陷（3 过期/错误断言 + 1 harness 错位），零产品缺陷。修复方向 = 改测试（R141-F2 / R141-F3），预期恢复绿基线（7979+4 = 7983 通过）。**

| 红测 | 最终判定 | 主审（复核确认） | 佐证维度 | 关键证据 |
|---|---|---|---|---|
| #1 `test_legacy_handoff_is_thread_affine_and_stale_receipts_are_not_reused` | **过期/错误断言（非产品缺陷）** | D1-1、D5-1 | D2/D3/D4/D6/D8 notes 一致 | `settle_reservation` 契约为"成功 handoff 的纯记账"：docstring 明写 "**without filter I/O**"（dupefilter.py:2288-2294），实现只移除收据不触碰 filter；全文件唯一 `self._filter.remove` 调用点在 `forget()` 补偿路径（:2363）；调度器在 durable push 成功后才调用 settle（scheduler.py:3098-3106）。实测：owner settle 返回 True/peer False（线程亲和正确）、settle 后同指纹 `request_seen` 返回 True（marker 正确保留）。同文件**通过**的 `test_late_legacy_settlement_after_clear_is_a_noop` 断言 `remove_calls == 0`，与失败断言直接互证。按测试"修产品"= 每次成功入队删 marker = 去重彻底失效（真实 P1 级破坏） |
| #2 `test_clear_retire_marks_legacy_receipt_and_clears_volatile_state` | **过期/错误断言（非产品缺陷）** | D1-1、D5-2 | D3/D6 notes 一致 | setup 用 `request.replace()`（同 URL → 同指纹，实测 fingerprint 相等），前一行 legacy `request_seen` 已**急切 add** marker（dupefilter.py:1943）；随后事务性调用只读检查 membership → seen=True → `_discard_reservation`（:1836-1840，finding 引 1848-1852 有轻微行号漂移，语义无误）。实测 `decision = DedupDecision(seen=True, reservation=None)`——混用两协议且同指纹时这是**跨协议去重的正确语义**；测试 setup 误以为 replace() 产生新指纹。修正 setup（换 URL）后其后断言（retired tombstone、volatile 清空）全部成立（实测） |
| #3 `test_filter_backend_failure_is_static_and_does_not_expose_driver_text` | **过期/错误断言（非产品缺陷）** | D1-1、D5-3 | D3 notes（脱敏性质本身成立：`_static_backend_error` 重建 CircuitBreakerOpenError/BackendConnectionError 静态实例，`monitor_events == []` 断言已通过） | 产品**有意**降级不记收据：注释明写 "filter-full and transient-outage degradation deliberately admit without writing"（dupefilter.py:376-380）；BackendConnectionError 分支返回 `(False, None, False)`（:1972-1990）→ reservation_state=None → 不创建收据（:1840-1841）；调度器 rollback gate 对 nothing-recorded 情形有 guard（scheduler.py:3063-3064）。同文件**通过**的姊妹测试 `test_backpressure_preserves_backend_error_degradation` 断言 `_legacy_reservations == {}`。语义自洽：降级时 marker 未写，无需 settle/forget 补偿。**附加提醒**：因 :432 前置断言失败，标题承诺的"driver 文本不泄漏"断言（:433-436）从未执行——修复时必须让这些断言真正跑起来 |
| #4 `test_scheduler_close_queue_worker_retries_observer_and_releases_manager` | **测试基建缺陷（非产品缺陷）** | D1-2、D5-4 | D2 notes（用修正 harness 实证产品正确：调度器重试观察者挂接成功、close 完成、manager.close 恰一次、lifecycle=closed） | harness 从未把 `_RejectingDeferred` 交给调度器：`_Workers.submit`（:63-72）每次新建 Deferred 并返回，:402 预置对象永远到不了调度器 → 首个 addCallbacks 实际**成功**（拒绝/重试分支 scheduler.py:2672-2683 从未进入，产品重试逻辑零覆盖）；:418 手动 `queue_worker.callback(None)` 不触达任何调度器回调；`_complete_worker(workers, 1)` 执行的是 `_release_managers`，`queue.close`（calls[0]）从未被执行 → `assert_called_once_with()` 报 "Called 0 times"（实测 0 次）。产品的观察者重试契约存在且正确 |

---

## 5. 被驳回项

| 候选 | 原始定级 | 裁决 | 驳回理由（复核员） |
|---|---|---|---|
| **D7-1**：sdist 产物泄漏合并备份 `pyproject.toml.orig`（uv_build 钉死版无条件打包该文件且 source-exclude 无法排除，CI 内容门禁正则也不拦 `*.orig`） | P2 | **REFUTED** | 表象属实（dist 内确有 .orig 且与当前 pyproject 逐字节一致）但**机制被实证推翻**：用钉死后端（uv_build 0.12.5）做隔离探针证明——(1) 根目录完全不存在 .orig 的最小项目，sdist 仍含 `pyproject.toml.orig`：该文件是 **uv_build 自己生成的**（后端规范化 pyproject.toml 并把原始文件保存为 .orig），本仓库 dist 中 .orig(12160B) == 构建时 pyproject、sdist 内 pyproject(8374B) 为规范化版，完全吻合；(2) 在根目录放置含 `SECRET_MARKER_1234` 的 .orig 后构建，标记内容**完全未进入** sdist——"编辑会话先写敏感内容再净化的 .orig 被无声打包"泄漏通道不存在；(3) source-exclude 对真实工作区文件有效（对照实验 docs/** 被正常移除），"dirty checkout 防泄漏契约被绕过"不成立。该文件是后端设计行为：每次构建（含 CI git archive HEAD 干净构建）都合法存在，内容仅镜像本就要发布的 git 跟踪 pyproject。**其修复建议反而有害**：ci.yml:159 正则加 `\.orig` 会令每次 CI 构建必然失败；`test ! -e pyproject.toml.orig` 前置守卫无法移除后端生成文件。附带 `.omc` 正则不对称最多 P3 纵深防御 nitpick（source-exclude 已实证可剔除），不足以独立支撑立案 |

降级（DOWNGRADED）：无。账本重复（DUPLICATE-LEDGER）：无。未复核（skeptic 缺失）：无（8/8 维度全部附完整裁决）。

---

## 6. 各维度已核查干净面（notes 汇总）

**D1 并发与生命周期**：锁序/死锁（dupefilter 全链仅 `_filter_operation_lock→lifecycle` 一种嵌套序，open()/clear() 均在生命周期临界区后取 filter 锁）；断路器（epoch 准入栅栏、HALF_OPEN 单探针含 KeyboardInterrupt 释放槽、reconnect new_generation 隔离）；GenerationLeaseGate（重入 disconnect finalizer 延迟、控制异常保留后仍完成权威 drain）；ConnectionManager（插件代码在 registry 锁外跑、retirement finalizer 0.01s 轮询不搁浅等待者、R140-F2 已验证 LANDED）；R138-F4（early-setup 注册键分叉）已验证在 WIP 中修复；调度器 open/close 状态机（attempt token 防陈旧回收、settlement barrier、enqueue 全部 BaseException 清理臂逐一补偿兄弟资源）；pipeline（_async_tail FIFO、同线程 close/store 自锁检测、manager 先记后回调防双释放）；reactor 适配器（authoritative/bounded 双视图不取消已接受 worker）；_plugin_contract（token+settling 预约防并发结算）。自查 REFUTED：settle 应删 marker / clear 应等 pending 收据 / 死代码 return None / MemoryMembershipFilter 无锁。

**D2 消息投递语义**：Redis Lua 原子 push/pop 与孤儿成员回收；MongoDB find_one_and_delete + 毒文档隔离；ES 乐观锁删除 pop 三竞态；RabbitMQ confirm 发布 + durable fail-closed（R139-F2/F3 在位）+ clear 拒绝 pending deliveries；SQS receipt/epoch 结算、purge 60s 窗口屏障、毒消息 best-effort 删除；Pulsar 泵/retirement/ack-token 单次结算；RocketMQ 消费代际令牌与 10s 下限 nack；Kafka 世代/纪元/水位提交与 nack-seek 保留 in-flight；priority/work_stealing tombstone 门（R133-F2 在位）；delay/time_wheel deadline 预算循环、drain 转移回滚、全有或全无快照恢复（restore 读失败→fence 在位）；round_robin/ring_buffer 快照回滚；_BoundQueueAckToken 跨代隔离；manifest-last 提交、bounded/quiescent GC；volatile 替代推送 fail-closed。REFUTED：原子后端 pop→repush 循环（设计意图）；throttle gate_lock 内阻塞（文档化）；close 无界等待 replacement settlements（权威性契约）；chunk_bytes 缩小旧 manifest 不可读（fail-closed 设计）；delay=0 回落（文档化）。

**D3 安全与脱敏**：全部动态后端错误消息均在静态终界装饰器内（9 后端 allowlist 全静态、dynamodb `return False` fail-closed）；42 个日志站点全静态；`_redact()` 秘密包裹覆盖全部 10 后端（含 Kafka SASL/Confluent、RocketMQ Credentials）；AWS 链（both-or-neither、ambient chain 显式、远程 HTTP+显式凭据拒绝、UNSIGNED+ignore_configured_endpoint_urls、策略先于主机规范化；对抗探针 0177.0.0.1/0x7f.1/2130706433/%6cocalhost/0.0.0.0/[::]/userinfo/双端口/空尾 ?#/大写 scheme 全部 fail-closed）；_endpoint_validation 对抗探针全拒；物理命名（kafka/sqs v2 长度前缀 blake2s、R138-F1 一致、MongoDB/ES/RabbitMQ 能力索引隔离 R45 在位）；191 个聚焦脱敏测试全过。REFUTED：ES push `str(e)` 泄漏（终界替换）；裸驱动异常带 traceback 逃逸（文档化 fail-loud）；短值过假红（RedactedBaseSettings 边界保留 allowlist 原消息）；hostile `__class__` isinstance 逃逸（文档化政策）。

**D4 设置与校验契约**：默认值无漂移（reactor 5/60、monitor 1000/60/86400、item 1MiB×3、pipeline_max_storage_errors=10 单常量贯通、快照 128MiB/256KiB/4096、redis redirects 5/0-100、SQS 300/1-43200、RocketMQ invisible≥10 与 nack 一致）；env 覆盖语义一致（规范化布尔/整数/浮点、Scrapy flat > env 优先级、未知 env/flat 名 fail-fast）；全部 10 后端 capture-and-revalidate 快照；端点文法层无新缺口；452 + 1904 settings 相关测试通过。REFUTED：env 回退被无关 SCRAPY_* 炸掉；SAMPLE_EVERY=0 绕 ge=1；Kafka acks 布尔混入；RocketMQ send_timeout=0 无界 deadline；SASL_PLAINTEXT/OAUTHBEARER 入 Literal 是有意设计（并入 D4-6/R141-F18）。

**D5 测试健康与覆盖**：除两个已知红测文件外全部新增/修改 WIP 测试聚焦通过（~1000 例，不存在第 5 个 WIP 红测）；无 xfail 滥用、55 skip 由 integration 分层/lazy-import 解释且有 test_collection_gates 守护；dupefilter 三个 warn-once 闩在乱序下无跨文件污染（聚焦乱序 190 例通过）；属性测试全部 derandomize；public_api.py 子进程 strict mypy 驱动；tools/check_audit_waiver.py 有覆盖；src 96 模块全部被测试 import、最大模块（spider_mixin 85.6%/scheduler 89.9%/kafka 87.9%）无断崖空洞、新模块各有专属测试；~78 个无 assert 冒烟与 ~15 处安全方向 time.sleep 为预存在弱模式。REFUTED：红测 2 或为产品缺陷；闩污染乱序 flake；红测 1 或为 TDD 目标规格。

**D6 文档一致性**：默认值/边界/设置名/统计键（21 个）/能力矩阵/异常树/BackendQueue 直构/settle-无-filter-IO 与 README 队列-先于-标记保证/examples 目录/快照 v3v2 逻辑键/get_queue_async/reset_snapshot/abort(reason) 全部逐条对照一致；CHANGELOG 的 Memcached IPv4/2.17 依赖锁/FutureWarning/私有 boto3 Session/60s SQS barrier 抽查属实。REFUTED：README localhost vs 127.0.0.1 绑定；redis-py 7.3-8.x vs extra >=8.1,<9（兼容域 vs 安装域）；"eleven bundled settings models"（10+base 成立）；"all nine connect()-capable backends"（历史条目时点）。

**D7 打包与 CI**：uv lock --check 通过（CI 钉死 uv 0.11.29 接受 revision 3）；extras all = 10 后端精确并集、wheel METADATA 一致、requires-python/CI 矩阵/classifiers/.python-version/ruff target 全对齐；entry points 组一致、CI 冒烟 30 名字全在 `__all__`；wheel .py 清单与 src 树 diff 为空、sdist 必备 10 项在位、docs/insight 正确排除；仓库卫生门禁通过；examples 硬编码 10 spider 一致；dependabot 覆盖 uv+actions。REFUTED：D7-1（见 §5）；uv revision 3 拒绝；WIP 后 lock 漂移；未跟踪垃圾进 sdist；本地 coverage.json 32.7% 误导。U20 引用（pyproject:69/SECURITY.md:170 → EXECUTION-INDEX.md）判为过弱不报。

**D8 公共 API 与类型**：顶层与全部子包 `__all__` ↔ 运行时导出程序化核对无缺漏/幽灵导出；PEP 562 `__getattr__/__dir__` 缺依赖判定链自洽；mypy --strict src 重跑全绿；下游 strict fixture 与 API 文档契约测试全过；py.typed 随 wheel、新模块在 wheel 内；README/migration-guide/runbook 全部导入可解析；五组件公共方法 Args 段与签名比对无失配（唯一失配即 R141-F9）；废弃垫片实测存活（RedisMode.MASTER_SLAVE 告警、resolve_circuit_breaker_policy 别名）。不报项：带注释的 cast type: ignore（stub 漂移护栏）、circuit_breaker abstract ignore（有完整说明）、SetOutcomeIndeterminateError 层级不对称（STABILITY 明文）、dupefilter: Any Scrapy 鸭子缝（docstring 声明）、空命名空间子包、DedupDecision 未入根 `__all__`。

---

## 7. 建议追加到 `docs/insight/LEDGER.md` 的行（Round R141）

> 状态说明：以下 23 条均为本次对抗复核 CONFIRMED 且**尚未修复**的发现，state 记为 `OPEN`（沿用账本允许的状态扩展先例：DIRTY-BLOCKED/BLOCKED-push/IDLE）。修复工作流落地后应改记 LANDED/REFUTED。R141-F10 同时是对 R138-F5（DIRTY-BLOCKED）的再确认——落地后应同步更新 R138-F5 行。

```markdown
| R141 | F1 snapshot-restore-resolution-failure-bypasses-fence | queue/queue.py:2099-2103,1937-1972 | snapshot-restore-resolution-failure-bypasses-fence | LANDED (fixed in fix waves 1-3, 2026-08-23) | P1 (CONFIRMED×2). Startup transient get_storage_backend failure swallowed by non-strict _snapshot_storage (indistinguishable from storage-incapable) → no fence on _restore_snapshot; close then overwrites authoritative manifest with empty state and deletes the never-read legacy key — ALL held items of stateful strategies silently lost (end-to-end repro: 149B manifest → empty state=None). Fix: distinguish resolution_failed (fence) from NotImplementedError (no-op). See DEEP-INSIGHT-2026-08-23-workflow R141-F1. |
| R141 | F2 stale-test-assertions-dupefilter-receipt-protocol | tests/test_dupefilter_connector_closure.py:150-162,406-418,426-437 | stale-test-assertion-dupefilter-receipt-protocol | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED; merges D1-1+D5-1/2/3). Red tests 1/2/3 contradict the implemented receipt protocol: settle is pure bookkeeping without filter I/O; replace() same-fingerprint transactional seen-hit discards reservation by design; degradation deliberately admits without writing receipts. Fix tests, NOT product (implementing the assertions would disable dedup / create ghost receipts). Unblocks green baseline. |
| R141 | F3 test-harness-deferred-delivery-misalignment | tests/test_scheduler_pipeline_async_coverage.py:54-70,396-428 | test-harness-deferred-delivery-misalignment | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED; merges D1-2+D5-4). Red test 4: _Workers.submit always news a Deferred, the pre-seeded _RejectingDeferred never reaches the scheduler, queue.close (calls[0]) never executes; product observer-retry contract (scheduler.py:2672-2683) has zero coverage. Corrected harness proves product correct (close completes, manager.close once, lifecycle=closed). |
| R141 | F4 redaction-schemeless-userinfo-bypass | exceptions/base.py:98-110; backends/_redaction.py:94-105 | redaction-schemeless-userinfo-bypass | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED×2). urlsplit username check is dead code without scheme:// — 'user:pw@host:port' passes both redaction heuristics (repro: BackendConnectionError retains 'user:hunter2@…' in str(e) and backend_type). Fix: structural x:y@ prefix check in both helpers. R134 CLEAN predates the WIP rewrite of both files. |
| R141 | F5 redis-blank-credential-accepted | settings/redis.py:394-401,469-479 | redis-blank-credential-accepted | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED×2). Redis username/password/sentinel_* accept blanks (all 9 sibling backends fail fast); empty password silently skips AUTH, blank username → opaque ACL failure, has_authentication half-applies TLS. Fix: strip-aware validators mirroring R32-A. |
| R141 | F6 confluent-mode-drops-validated-tls-material | settings/kafka.py:370-384,551-578; backends/kafka.py:1179-1191 | confluent-mode-drops-validated-tls-material | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED×2). CONFLUENT early-return dict omits ssl_cafile/cert/key (consumer/admin never see CA; producer only via inconsistent residue) and silently overrides security_protocol; validated private-CA pinning silently fails. Violates ES CLOUD fail-fast precedent. Fix: model_validator rejection or real passthrough. |
| R141 | F7 changelog-stale-batched-clear-description | .github/CHANGELOG.md:326-340 | changelog-stale-batched-clear-description | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED×2). Unreleased Breaking bullet still describes the superseded DynamoDB BatchWriteItem-bounded clear (25/batch, 7 jitter retries, 8 submissions); implementation is per-row conditional DeleteItem(ALL_OLD) and the newer bullet at :45-51 contradicts it. Fix: rewrite/delete the stale bullet before release. |
| R141 | F8 fanout-v2-default-flip-unreachable-drain | queue/strategies/_names.py:26-128; schedule/scheduler.py:973-987; schedule/_queue_config.py | fanout-v2-default-flip-unreachable-drain | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED; merges D8-1+D2-3). WIP flips fan-out physical names to v2 by default with no settings knob (migration-guide's legacy_v1 drain unreachable via from_settings), no CHANGELOG Breaking entry (violates STABILITY), stale 'backlog-compatible' docstrings at priority.py:142/work_stealing.py:163; upgrades silently switch queues and strand backlog. Fix: changelog entry + SCRAPY_QUEUE_NAME_GENERATION knob (or one-shot migration warning) + docstring fix. |
| R141 | F9 documented-attribute-missing-on-instance | queue/queue.py:155-163; pipeline/pipeline.py:158-171 | documented-attribute-missing-on-instance | LANDED (fixed in fix waves 1-3, 2026-08-23) | P2 (CONFIRMED×2). Stable-tier docstrings declare BackendQueue.serializer/spider and BackendPipeline.serializer but instances hold only _serializer/_spider — AttributeError on documented access (runtime-verified; pre-existing at HEAD). Fix: drop docstring entries or add read-only properties. |
| R141 | F10 set-monitor-upgrade-drops-pop-rate-window | spider/spider_mixin.py:1651-1655; queue/queue.py:382-387 | set-monitor-upgrade-leaves-frozen-window | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). Re-verification of R138-F5 (DIRTY-BLOCKED): still unfixed in the current tree; live repro shows window frozen at 60.0 vs configured 300.0 after the early-setup NullMonitor upgrade (SCRAPY_MONITOR_POP_RATE_WINDOW_S dead on that path). Fix: optional pop_rate_window_s param on set_monitor + passthrough. Supersedes R138-F5 evidence; update that row when LANDED. |
| R141 | F11 legacy-add-before-receipt-publish-baseexception-window | dupefilter/dupefilter.py:1840-1847,1943 | legacy-add-before-receipt-publish-baseexception-window | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). BaseException between filter.add (marker written) and legacy receipt publication leaves an uncompensated ghost marker permanently suppressing the URL (repro via injected KeyboardInterrupt; transactional path has the symmetric owner-intent protocol). Fix: pre-publish intent + one-shot retry allowance. |
| R141 | F12 kafka-ack-postcommit-interruption-attempt-leak | backends/kafka.py:2435-2438,2470-2505 | kafka-ack-postcommit-interruption-attempt-leak | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). Interruption between successful commit and post-commit bookkeeping strands _active_attempts entries; retried acks early-return at the in-flight guard and legacy bare ack is refused on that partition until rebalance (repro'd). Fix: _finish_attempt_locked before the early return. |
| R141 | F13 empty-secret-needle-interleaves-redaction | exceptions/base.py:694-695 | empty-secret-needle-interleaves-redaction | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). ConfigurationError with SecretStr('')/'' under a sensitive name runs str.replace with an empty needle, interleaving ***REDACTED*** between every character (repro'd); main loop guards at :284, secret branch does not. Latent (bundled validators reject blanks first). Fix: mirror the guard. |
| R141 | F14 sqs-ack-token-repr-renders-receipt-handle | backends/sqs.py:394-401 | sqs-ack-token-repr-renders-receipt-handle | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). _SqsAckToken.__repr__ renders real receipt handles verbatim despite its masking comment (repro'd with a live-shaped handle); Pulsar masks message_id unconditionally. Fix: unconditional receipt_handle=<redacted>. |
| R141 | F15 settings-error-message-safelist-coverage-drift | settings/_redacted.py:34-236 | settings-error-message-safelist-coverage-drift | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). R67 root-class recurrence: static f-string ConfigurationError messages (redis address / kafka SASL variants / ES basic-auth) collapse to the generic message; runbook diagnostic text unreachable via public paths. Fix: extend safe-list + enumeration contract test. |
| R141 | F16 rabbitmq-cluster-config-dead-and-half-applied | settings/rabbitmq.py:260-267; backends/rabbitmq.py:839-860 | rabbitmq-cluster-config-dead-and-half-applied | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). cluster_node_type is dead config (zero consumers, R25-H precedent); STANDALONE+cluster_nodes silently ignored for connecting but counted for TLS/guest classification (verified). Secondary: mongodb atlas_cluster_name dead. Fix: delete dead field (tombstone) + reject non-selected topology input. |
| R141 | F17 kafka-group-id-blank-accepted | settings/kafka.py:440-443 | kafka-group-id-blank-accepted | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). KafkaSettings(group_id='   ') constructs; blank reaches KafkaConsumer verbatim (siblings reject blanks for identity fields). Fix: field_validator + RED test. |
| R141 | F18 kafka-settings-document-unavailable-values | settings/kafka.py:440-455,327-333,346-356 | kafka-settings-document-unavailable-values | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). enable_auto_commit=True documented as available but rejected at backend construction (wrong failure layer); SASL_PLAINTEXT/OAUTHBEARER listed in Literal/descriptions but always rejected without notice. Fix: settings-layer rejection + honest descriptions. |
| R141 | F19 readme-forbids-init-setup-backend-vs-error-messages-demand-it | README.md:1096; spider/spider_mixin.py | readme-forbids-init-setup-backend-vs-error-messages-demand-it | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). README forbids setup_backend() in __init__ while the class docstring example and 4 runtime error messages instruct it (both paths work — from_crawler is idempotent). Fix: align one side. |
| R141 | F20 ci-audit-report-never-uploaded | .github/workflows/ci.yml:571-576 | ci-audit-report-never-uploaded | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). 'Publish audit report' (if: always()) writes audit-report.txt to the ephemeral runner with no upload-artifact step anywhere — claimed artifact retention does not exist. Fix: upload-artifact@v4 or delete the step. |
| R141 | F21 new-async-lifecycle-apis-return-any | dupefilter/dupefilter.py:1355-1425 | new-async-lifecycle-apis-return-any | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). WIP public open_async/clear_async/release_async annotated -> Any though defer_to_thread_ordered is precisely typed and get_queue_async already uses Deferred[T]; downstream strict users lose all checking. Fix: Deferred[None] via TYPE_CHECKING import. |
| R141 | F22 exported-lease-breaker-apis-tierless-changelogless-any-typed | backends/connectors/__init__.py:21-23; connectors/_manager.py:953,2659,2975 | exported-lease-breaker-apis-tierless-changelogless-any-typed | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2; skeptic corrected: pre-existing at HEAD, not WIP-added). ConnectionManagerLease / acquire_lease / release_manager_acquire / apply_scrapy_breaker_policy are public exports with no STABILITY tier, no CHANGELOG entry, and owner/settings: Any duck-typing — 'public but tier-less' limbo per the repo's own rule. Fix: assign Experimental tier + changelog + Protocol typing. |
| R141 | F23 monitor-docstring-hook-list-incomplete | monitor/base.py:119-121 vs 307,324 | monitor-docstring-hook-list-incomplete | LANDED (fixed in fix waves 1-3, 2026-08-23) | P3 (CONFIRMED×2). Monitor class docstring hook inventory (the de-facto additive contract list) omits the defined on_buffer_depth/on_delay_depth gauges. Fix: add both entries. |
```

被驳回（不入账本）：D7-1 pyproject.toml.orig sdist "泄漏"——REFUTED（uv_build 设计行为，.orig 由后端在构建时自行生成、内容仅镜像 git 跟踪的 pyproject，敏感内容通道实证不存在，source-exclude 对真实文件有效；详见 §5）。

---

## 8. 修复阶段分组建议（CONFIRMED 项按文件/子系统聚类）

每组"小而精"（≤3 条合并条目），独立可交付；统一验证底线 = 聚焦测试 + 全量门禁（`ruff` / `mypy --strict src` / 全量 `pytest`，修复红测组后应达 7983 全绿）。建议顺序：G1/G2 先行恢复绿基线 → G3（唯一 P1）→ 其余按组并行。

| 组 | 发现 | 文件/子系统 | 修复要点 | 验证方式 |
|---|---|---|---|---|
| **G1 红测修复①：dupefilter 收据协议断言** | R141-F2（D1-1+D5-1/2/3） | tests/test_dupefilter_connector_closure.py | 三处断言改为协议正确语义（remove_calls==0+marker 保留断言；setup 换 URL；`_legacy_reservations=={}`+泄露检查移至 monitor 事件/日志并确保执行） | `pytest tests/test_dupefilter_connector_closure.py`（35 例全绿）；`pytest tests/test_dupefilter_lifecycle.py`（兄弟契约不回归）；全量 pytest + ruff + mypy --strict src |
| **G2 红测修复②：scheduler close harness** | R141-F3（D1-2+D5-4） | tests/test_scheduler_pipeline_async_coverage.py | submit 弹出预置 Deferred 或 monkeypatch `_submit_thread`；`_complete_worker(workers, 0)` 真实驱动 queue.close；断言 addCallbacks 拒绝→重试路径被真实触达、queue.close/manager.close 各恰一次 | `pytest tests/test_scheduler_pipeline_async_coverage.py` 全绿；确认重试分支覆盖（可临时探针验证）；全量门禁 |
| **G3 快照恢复围栏（唯一 P1）** | R141-F1（D2-1） | queue/queue.py（`_snapshot_storage`/`_restore_snapshot`） | 区分 unsupported 与 resolution_failed；resolution 失败设 `_snapshot_persistence_fenced=True`；NotImplementedError 保持 no-op | 新增聚焦回归测试（启动解析失败→storage 恢复→close 不覆盖 manifest、不删 legacy key；对照组正常恢复）；`pytest tests/test_queue_resilience.py` + 快照相关测试；全量门禁 |
| **G4 脱敏/诊断强化** | R141-F4、F13、F14（D3-1/2/3） | exceptions/base.py、backends/_redaction.py、backends/sqs.py | schemeless `x:y@` 结构检查（两处 helper）；空秘密 needle 守卫；`receipt_handle=<redacted>` 无条件 mask | `pytest tests/test_redaction.py tests/test_operation_error_redaction.py` + 新增 schemeless-userinfo/空秘密/receipt-handle 用例；全量门禁 |
| **G5 Redis 凭据校验** | R141-F5（D4-1） | settings/redis.py（+validate_redis_transport_security） | strip-aware username/password/sentinel_* 校验（R32-A 同形，错误不带 setting_value） | `pytest tests/test_settings_validation.py tests/test_backend_settings_contracts.py` + 新增空白凭据 RED→GREEN 用例；全量门禁 |
| **G6 Kafka 设置契约** | R141-F6、F17、F18（D4-2/5/6） | settings/kafka.py | CONFLUENT TLS 物料/协议 fail-fast（或真透传）；group_id 非空校验；enable_auto_commit 上移 settings 层拒绝 + 描述诚实化（含 SASL_PLAINTEXT/OAUTHBEARER 标注） | `pytest tests/test_settings_validation.py tests/test_backend_settings_contracts.py` + 新增三组用例；新消息同步 safe-list（联动 G8）；全量门禁 |
| **G7 Kafka 后端簿记** | R141-F12（D2-2） | backends/kafka.py（ack in-flight 守卫分支） | 偏移已不在 in-flight 的提前 return 前 `_finish_attempt_locked(token, handles)`（保留 handles 身份校验） | Kafka 聚焦测试（ack 重试/中断模拟用例）；全量门禁 |
| **G8 settings 消息 safe-list 契约** | R141-F15（D4-3） | settings/_redacted.py（+契约测试） | 补齐 redis/kafka/ES 静态变体；新增"settings 模块新增 ConfigurationError 消息必须同步 safe-list"枚举契约测试（同时收录 G5/G6 新消息） | `pytest tests/test_settings_message_safelist_contract.py` + 新契约测试；runbook 症状文本抽查；全量门禁 |
| **G9 RabbitMQ/Mongo 死配置清理** | R141-F16（D4-4） | settings/rabbitmq.py（+settings/mongodb.py 次要项） | 删除 cluster_node_type（留 "do NOT re-add" 墓碑）；STANDALONE+cluster_nodes raise（Redis 模式意图拒绝同形）；可选同批处理 atlas_cluster_name | `pytest tests/test_settings_validation.py`（枚举测试同步）；全量门禁 |
| **G10 fan-out v2 迁移契约** | R141-F8（D8-1+D2-3） | queue/strategies/_names.py、schedule/_queue_config.py、CHANGELOG、README/migration-guide、priority.py/work_stealing.py docstring | CHANGELOG Breaking 条目；`SCRAPY_QUEUE_NAME_GENERATION` 受校验旋钮（{v2, legacy_v1}）；或一次性迁移 warning；修正两处 "backlog-compatible" docstring；文档主指令与可达配置面对齐 | `pytest tests/test_queue_strategies*.py` + 新旋钮校验测试；文档对照（migration-guide/runbook/README 步骤可执行）；全量门禁 |
| **G11 公共属性/hook 文档契约** | R141-F9、F23（D8-2/5） | queue/queue.py、pipeline/pipeline.py、monitor/base.py | 删除（或补 property 实现）不存在的 Attributes 条目；Monitor hook 清单补 on_buffer_depth/on_delay_depth | 扩展 `tests/test_api_documentation_contract.py`：断言文档化 Attributes 可访问、hook 清单与方法集一致（防再犯）；全量门禁 |
| **G12 文档对齐** | R141-F7、F19（D6-1/2） | .github/CHANGELOG.md、README.md（对照 dynamodb.py、spider_mixin.py） | 删除/改写 DynamoDB 陈旧 bullet；README setup_backend 指引与 docstring/4 处错误消息二选一对齐 | 文档契约测试/人工对照（bullet 与实现互斥消除；错误消息指引一致）；全量门禁 |
| **G13 dupefilter 类型与补偿窗口** | R141-F21、F11（D8-3/1-4） | dupefilter/dupefilter.py | open_async/clear_async/release_async 标注 Deferred[None]（TYPE_CHECKING）；legacy add→发布间隙 pre-publish intent + 一次性 retry allowance（或 close/clear 兜底记录） | mypy --strict src + reveal_type 下游 fixture；`pytest tests/test_dupefilter_lifecycle.py` + 新增 BaseException 窗口回归测试；全量门禁 |
| **G14 spider_mixin monitor 窗口透传** | R141-F10（D1-3，R138-F5 再确认） | spider/spider_mixin.py、queue/queue.py | `set_monitor` 可选 pop_rate_window_s 参数并在 :1653 透传 `monitor, window = _resolve_queue_monitor()`（或专用 set_pop_rate_window） | `pytest tests/test_spider_mixin.py -k early_setup` + 新窗口升级用例（升级后 window==300.0）；落地后同步更新 LEDGER R138-F5 行；全量门禁 |
| **G15 CI artifact 留存** | R141-F20（D7-2） | .github/workflows/ci.yml | 补 `actions/upload-artifact@v4`（if: always(), path: audit-report.txt）或删除误导步骤 | YAML lint/工作流契约测试；grep 确认 upload 存在或步骤删除；全量门禁 |
| **G16 connectors 公共面定级** | R141-F22（D8-4） | STABILITY.md、CHANGELOG、connectors/_manager.py、connectors/__init__.py | STABILITY 表为 4 个 lease/breaker 名字指定层级（建议 Experimental）+ CHANGELOG Added；settings/owner Any 收紧为 Protocol/联合类型 | STABILITY/CHANGELOG 契约测试（枚举导出名 vs 表）；mypy --strict src；全量门禁 |

**依赖与顺序提示**：G8 依赖 G5/G6（新 ConfigurationError 消息需同步 safe-list，可并入同一 PR 序列）；G10 与 G12 都触碰 CHANGELOG（不同段落，可并行但需协调 rebase）；G1/G2 与其余组无代码耦合，建议最先合并以恢复 CI 绿信号。
