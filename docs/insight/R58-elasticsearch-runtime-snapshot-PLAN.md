# R58 PLAN

Confirm mutable configuration can downgrade authenticated transport and collapse
capability indices, introduce a validated immutable client-generation snapshot,
route all operations through it, add regressions, verify, and make one atomic
commit.
