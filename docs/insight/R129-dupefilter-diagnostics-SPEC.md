# R129 SPEC

Dupefilter graceful misses, linearized reservations/retry decisions, monitor fallbacks, and MemoryFilter LRU/saturation updates retain their semantic results despite diagnostic failure; direct filter, backend, and monitor controls remain observable.
