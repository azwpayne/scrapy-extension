# R76 PLAN

Confirm producer close can skip admin cleanup and mask connect failure, make
failed cleanup attempt all detached handles while suppressing secondary control
errors, add identity regressions, verify, and atomically commit.
