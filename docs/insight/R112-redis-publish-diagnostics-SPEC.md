# R112 SPEC

Once a Redis client generation is published, its success diagnostic cannot make `connect()` report failure or expose a control signal; genuine construction and publication failures remain observable.
