# PLAN — Round 2 Hardening (2026-06-25)

Phased execution. **Tier 1 = 5 file-disjoint work units** (parallel-safe fan-out). See
[`INSIGHTS-2026-06-25.md`](./INSIGHTS-2026-06-25.md) for evidence,
[`SPEC-2026-06-25.md`](./SPEC-2026-06-25.md) for goals/constraints.

## STATUS — live tracker (branch `fix/hardening-r2-distributed-seams`)

| Unit | Scope | State |
|---|---|---|
| A | C1 ack-capability gate + C3 SQS correct-URL ack + C6 envelope + C8 `{spider}` template (scheduler.py owned here; E1/E2 folded in) | ✅ done — RED→GREEN, C3 reproduced |
| B | C2 pipeline escalation + monitor `on_store` | ✅ done — RED→GREEN |
| C | C4 batched-storage at-least-once flush | ✅ done — RED→GREEN, C4 reproduced |
| D | C5 dedup clear-on-open + `{spider}` template + factory warning | ✅ done — RED→GREEN |
| E | B2 breaker `queue_len` out of hot-path + B3 single half-open probe | ✅ done — RED→GREEN, B3 race reproduced (8 probes → 1) |
| — | Doc-accuracy: round-1 INSIGHT/PLAN ES-pop + CI-residual fixes | ✅ done |

**Central verify (orchestrator):** `1251 passed, 27 skipped, 0 failed` · `ruff check`: All checks passed! · `mypy src/scrapy_extension`: Success, no issues in 65 files · bandit: 0 issues. (Was 1203 → +48 new regression tests; 3 HYPOTHESIS gates — C3, C4, B3 — all reproduced RED pre-fix.)

---

## Tier 1 — parallel fan-out (disjoint files)

### Unit A — ack-capability contract + SQS correct-URL ack  *(C1 CRITICAL + C3 HIGH)*
**Files**: `src/scrapy_extension/backends/base.py`, `src/scrapy_extension/backends/{kafka,rabbitmq,sqs,pulsar}.py`,
`src/scrapy_extension/schedule/scheduler.py`, `tests/test_sqs_backend.py`, `tests/test_scheduler_ack_gate.py` (new).

**A1 — capability contract** (`backends/base.py` `QueueBackend`):
- Add class attrs: `requires_ack: bool = False` (True = pop yields a message needing explicit ack;
  False = atomic pop, ack is a no-op) and `supports_concurrent_ack: bool = True` (False = single-slot,
  unsafe under `CONCURRENT_REQUESTS>1`).
- Defaults make atomic-pop backends (Redis/Mongo/ES/RocketMQ) pass untouched.

**A2 — per-backend declaration** (the 4 MQ backends):
- `KafkaQueueBackend`/`RabbitMQQueueBackend`: `requires_ack = True`, `supports_concurrent_ack = True` (real in-flight set).
- `SqsQueueBackend`/`PulsarQueueBackend`: `requires_ack = True`, `supports_concurrent_ack = False`.

**A3 — scheduler gate** (`schedule/scheduler.py` `from_settings`, after the queue backend is resolved):
- Read `CONCURRENT_REQUESTS` from Scrapy settings (default 16). If
  `type(backend).requires_ack and not type(backend).supports_concurrent_ack` and
  `CONCURRENT_REQUESTS > 1` and not `SCRAPY_ACK_UNSAFE_CONCURRENT_REQUESTS` (env/setting, default false)
  → raise `ConfigurationError` with a message naming the backend + the opt-out.
- Docstring: correct the over-claim at `scheduler.py:67-69` — state the gate's exact contract.

**A4 — SQS correct-URL ack** (`backends/sqs.py`, the C3 bug):
- Replace single `_last_receipt: str | None` with `_last_receipt: tuple[str, str] | None` =
  `(queue_url, receipt_handle)`. `pop` records the URL it received from; `ack` deletes against that
  URL. Drop `del queue_name`; keep `token` unused (signature compat) until the real in-flight-set lands.
- **Behavior change**: multi-queue SQS now acks correctly. Single-queue unchanged.

**Tests (TDD — RED first)**:
- `test_scheduler_ack_gate.py`: SQS+`CONCURRENT_REQUESTS=16` → `ConfigurationError`; opt-out flag →
  passes; Kafka/RabbitMQ+16 → passes (real ack); Redis+16 → passes (atomic); atomic+1 → passes.
- `test_sqs_backend.py`: pop from qB → `ack` calls `delete_message(QueueUrl=qB_url, ReceiptHandle=…)`,
  NOT qA_url (RED pre-A4, GREEN post).

---

### Unit B — pipeline escalation + monitor `on_store`  *(C2 HIGH + F-asymmetry)*
**Files**: `src/scrapy_extension/pipeline/pipeline.py`, `src/scrapy_extension/settings/base.py`,
`src/scrapy_extension/monitor/stats.py` (verify hook signature only), `tests/test_pipeline.py`.

**B1 — escalation setting** (`settings/base.py` + `pipeline.py`):
- Add `SCRAPY_PIPELINE_MAX_STORAGE_ERRORS: int | None = None` (default None → current swallow-and-stat
  behavior, zero compat break). When set to N, the pipeline tracks consecutive `storage_errors`; at
  >N it **re-raises** (wrapped as `BackendError`) instead of returning the item.
- Reset the consecutive counter on a successful store.

**B2 — monitor wiring** (`pipeline.py`):
- Accept an optional `monitor: Monitor` in `__init__` (thread through `from_crawler` like dupefilter);
  call `self._monitor.on_store(key)` after a successful `_store_item`. Default `NullMonitor` when unset.

**Tests (TDD)**:
- default (`MAX_STORAGE_ERRORS=None`): storage raises → item returned + `storage_errors` incremented (preserve behavior).
- N=2: two consecutive raises → 3rd raises `BackendError`; a success between resets the counter.
- `on_store` invoked on success (mock monitor); not invoked on failure.

---

### Unit C — batched storage at-least-once flush  *(C4 HIGH)*
**Files**: `src/scrapy_extension/storage/strategies/batched.py`, `tests/test_storage_strategies.py`.

**C1 — flush ordering** (`batched.py` `_flush_to`):
- Snapshot the batch (`batch = list(self._buffer)`) under the lock, then clear `_buffer` — BUT wrap
  the per-item `backend.store` loop so that on the first `Exception`, the **un-written tail**
  (`batch[i:]`) is prepended back into `_buffer` (under the lock) before re-raising (or, if
  re-raising is a behavior change, increment a `storage/batched_flush_partial` stat and keep the tail
  buffered for the next flush — at-least-once, no silent loss).
- Preserve single-threaded-pipeline assumption; document the concurrency note already present.

**Tests (TDD — RED first)**: threshold=3, push 3 items, `backend.store` raises on item 2 → assert
item 3 is still in `_buffer` (re-enqueued) and the error is surfaced (raise or stat). Today item 3
is silently dropped (RED).

---

### Unit D — dedup lifecycle + key templating + factory warning  *(C5 HIGH + C-scope warning)*
**Files**: `src/scrapy_extension/dupefilter/dupefilter.py`, `src/scrapy_extension/dupefilter/filters/factory.py`,
`src/scrapy_extension/settings/base.py`, `tests/test_dedupe_factory.py`, `tests/test_dupefilter.py`.

**D1 — clear-on-open** (`settings/base.py` + `dupefilter.py`):
- `SCRAPY_DUPEFILTER_CLEAR_ON_OPEN: bool = False` (default off → zero compat break). When true,
  `BackendDupeFilter.open(spider)` calls `self.clear()` before the run.

**D2 — `{spider}` key templating** (`dupefilter.py` + `schedule/scheduler.py` queue_key path is Unit E;
here just dedup key):
- If the resolved dedup key contains `{spider}`, substitute `spider.name` at `open()`.

**D3 — per-process-scope factory warning** (`dupefilter/filters/factory.py`):
- When `SCRAPY_DEDUP_STRATEGY in {bloom, cuckoo, memory}`, emit a one-time `logger.warning` stating
  the filter is per-process and cross-worker duplicates will pass (point at the Set/Memory-via-backend
  alternative). Atomic, idempotent via a module-level flag.

**Tests (TDD)**:
- `clear_on_open=True`: add fp → seen True → `open()` → same request seen False.
- `{spider}` substitution: key `dupefilter:{spider}` + spider "foo" → resolved key contains "foo".
- factory: selecting `bloom` emits exactly one warning (caplog); `set` emits none.

---

### Unit E — scheduler envelope + key templating + breaker hardening  *(C6 HIGH + C8 HIGH + B2/B3 MEDIUM)*
**Files**: `src/scrapy_extension/schedule/scheduler.py`, `src/scrapy_extension/backends/circuit_breaker.py`,
`tests/test_scheduler_*.py`, `tests/test_circuit_breaker.py`.

**E1 — enqueue envelope** (`scheduler.py` `enqueue_request`):
- Move `self.dupefilter.request_seen(request)` INSIDE the existing try/except. On `QueueError`/
  `BackendError`: log + increment `scheduler/dupefilter_error` stat + treat as "not seen" (enqueue)
  OR skip — pick the safer of the two for a crawler (default: enqueue, so no URL is lost; document).

**E2 — `{spider}` queue_key templating** (`scheduler.py`):
- In `from_settings`/`open`, if `queue_key` contains `{spider}`, substitute `spider.name`. Default
  key unchanged (`"scheduler:queue"`) for backward compat; document the multi-spider footgun + the
  template opt-in in the docstring.

**E3 — breaker hot-path** (`circuit_breaker.py`):
- Remove `"queue_len"` from `_HOT_PATH` (admin/observability probe must not cascade to traffic
  shutdown). Keep `push`/`pop`.

**E4 — breaker single half-open probe** (`circuit_breaker.py`) — only if B3 RED test confirms the race:
- Hold the lock across the OPEN→HALF_OPEN transition AND the probe `func()` call (or use a
  dedicated `_probe_in_flight` flag) so only one thread issues the probe.

**Tests (TDD)**:
- E1: `request_seen` raises `QueueError` → `enqueue_request` does NOT raise (stat incremented).
- E2: `queue_key="q:{spider}"` + spider "foo" → backend push called with "q:foo".
- E3: `queue_len` raises N times while `push`/`pop` succeed → breaker stays CLOSED (RED pre-E3).
- E4 (only if RED confirms): N threads blocked on OPEN breaker, advance clock, release → exactly
  ONE `func()` probe call (RED pre-E4 if race real).

---

## Tier 2 — deferred (spec'd, not this pass)
- Real in-flight-set ack for SQS/Pulsar (replaces the Unit A gate).
- Backpressure action: scheduler consumes `queue/backpressure` → `DOWNLOAD_DELAY`/pause.
- Distributed Delay (durable heap) + distributed throttle (shared token bucket).
- Distributed Bloom/Cuckoo (shared sketch across workers).
- Entry-point plugin registration (`[project.entry-points."scrapy_extension.backends"]`).
- Security parity: `_RedactedStr` across all backends; Mongo `tls_allow_invalid_certificates` mode
  guard; ES http+creds cleartext guard; SQS/DynamoDB `endpoint_url` scheme allowlist; Pulsar TLS
  decoupling; Sentinel/Cluster `BackendConnectionError` wrap; AWS half-cred XOR validator.

## Tier 3 — doc / infra
- Doc-accuracy: mark round-1 INSIGHT.md "ES pop non-atomic" RESOLVED; PLAN.md "ES atomic pop" DONE;
  "integration CI commented out" RESOLVED. Add round-2 residual rows.
- `rocketmq-client-python` replacement spike (unmaintained 6y).
- Reconnect-after-close in-flight-state survival test (B5 HYPOTHESIS).
- Ack-handler body tests (test-eng HIGH gaps) — `_on_response_received`/`_on_spider_error` actually
  invoke `ack(token=…)`/`nack(token=…)`.

---

## Execution & verification

- **Fan-out**: Units A–E in parallel (disjoint files → no edit collision). Each executor:
  RED regression test (fails pre-fix) → minimal fix → run ONLY their own test file(s) to GREEN.
  Do **not** run the full suite mid-edit (concurrent noise); the orchestrator runs the single
  authoritative full-suite verify at the end.
- **HYPOTHESIS-gated units**: C3 (Unit A), C4 (Unit C), B3 (Unit E-E4) — the RED test MUST confirm
  the bug before the fix is applied. If the RED test does NOT fail pre-fix, the finding is wrong;
  downgrade severity in INSIGHTS and skip the fix (do not invent work).
- **Central verify (orchestrator)**: the full pytest + ruff + mypy command above; fix cross-unit
  fallout; append the green tally to this STATUS table.
- **No commit without user sign-off** (per round-1 protocol); present a consolidated result + diff.
