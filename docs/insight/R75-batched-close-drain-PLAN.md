# R75 PLAN

Confirm close can skip final drain after interrupted age-flusher join or a slow
threshold flush, preserve the first failure while acquiring the flush lock
through the close deadline, directly drain the tail, add deterministic
regressions, verify, and atomically commit.
