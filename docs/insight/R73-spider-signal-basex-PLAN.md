# R73 PLAN

Confirm a control exception skips sibling signal disconnects and can mask a
registration failure, complete every disconnect before re-raising the first,
isolate rollback logging, add deterministic regressions, verify, and atomically
commit.
