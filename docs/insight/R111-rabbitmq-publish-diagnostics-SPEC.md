# R111 SPEC

Once a RabbitMQ connection and channel generation is published, its success diagnostic cannot make `connect()` report failure; real construction, publication, and retirement controls remain observable.
