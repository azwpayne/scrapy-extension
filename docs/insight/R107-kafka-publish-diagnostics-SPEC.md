# R107 SPEC

Once Kafka producer and admin clients are published, every success diagnostic across all deployment modes cannot abort that complete generation; real construction failures retain abort cleanup.
