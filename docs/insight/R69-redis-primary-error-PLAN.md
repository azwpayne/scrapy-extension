# R69 PLAN

Confirm failed Redis candidate cleanup can replace a health-check control
exception, suppress cleanup control errors only in the unpublished abort path,
add an exception-identity regression, verify, and atomically commit.
