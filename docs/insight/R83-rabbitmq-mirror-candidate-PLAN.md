# R83 PLAN

Confirm mirrored candidate warning can interrupt before outer ownership and
leak handles, clean the private pair best-effort, add a deterministic warning
interruption regression, verify, and atomically commit.
