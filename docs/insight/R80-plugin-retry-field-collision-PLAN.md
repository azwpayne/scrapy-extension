# R80 PLAN

Confirm non-bundled settings split steals declared plugin retry fields, resolve
the selected plugin settings model only for field ownership, preserve explicit
manager aliases, add descriptor-backed regression coverage, verify, and commit.
