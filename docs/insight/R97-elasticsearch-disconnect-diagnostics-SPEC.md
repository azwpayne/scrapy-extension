# R97 SPEC

Elasticsearch normal disconnect remains best-effort after an ordinary client-close error even if its diagnostic logger interrupts; direct close control exceptions remain observable after detach.
