# R55 PLAN

Confirm the public Kafka disconnect sibling leak, add a deterministic
control-exception regression, implement close-all cleanup, verify, and make
one atomic commit. Keep partial-connect cleanup as the next isolated slice.
