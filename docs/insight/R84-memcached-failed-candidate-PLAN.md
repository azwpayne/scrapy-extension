# R84 PLAN

Confirm the failed-probe cleanup masks causal exceptions, isolate only that
private-candidate cleanup from normal close semantics, add deterministic
control-signal regressions, verify, and atomically commit.
