# R65 PLAN

Confirm overlapping Elasticsearch connects can overwrite a candidate and make
failure cleanup close another generation, keep the candidate private behind a
shared lifecycle lock, publish after complete initialization, add deterministic
connect/connect and connect/disconnect regressions, verify, and atomically
commit.
