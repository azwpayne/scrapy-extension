# R71 PLAN

Confirm a post-publication control exception leaves a closed client published,
centralize failed-connect rollback with identity-based detach and
primary-error-preserving cleanup, add deterministic post-publication
regressions, verify, and atomically commit.
