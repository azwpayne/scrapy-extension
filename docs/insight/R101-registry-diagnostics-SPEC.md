# R101 SPEC

Registry discovery availability must survive diagnostic-handler failures on enumeration errors, broken plugins, duplicates, and bundled-wins shadowing; plugin control exceptions remain observable.
